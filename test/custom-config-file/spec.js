const test = require('ava')

test('it handles a custom config file correctly when a valid config path is given', t => {
	// test happy path for thresholds and alternative
})
test('it shows a helpful error message when the custom config file cannot be found', t => {})
